import React from 'react';
import { Grid, Card, Image } from 'semantic-ui-react';

const ProfileCard = () => {
  return(
    <div>
      <Card>
      </Card>
    </div>
  )
}

export default ProfileCard;
